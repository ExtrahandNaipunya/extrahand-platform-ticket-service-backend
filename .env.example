# Server
PORT=8001

# MongoDB
MONGODB_URI=mongodb://localhost:27017/extrahand-support

# CORS — comma-separated allowed browser origins (frontend URLs)
# CORS_ORIGINS=http://localhost:3005,https://tickets.extrahand.in
# CORS_ORIGIN= (legacy single key; merged with CORS_ORIGINS)
# Public ticket portal URL (used for admin invite links: {WEB_APP_URL}/accept-invite?token=...)
# Production: https://tickets.extrahand.in
# WEB_APP_URL=http://localhost:3005
# FRONTEND_URL=https://tickets.extrahand.in

# Default portal admin (seeded on startup if user does not exist)
# ADMIN_EMAIL=admin@yourdomain.com
# ADMIN_PASSWORD=change-me-strong
# ADMIN_NAME=Support Admin
# ADMIN_ROLE=admin

# extrahand-email-service (must match SERVICE_AUTH_TOKEN there)
# EMAIL_SERVICE_URL=http://localhost:4007
# SERVICE_AUTH_TOKEN=your_super_secret_token_min_32_chars_long_12345
# TICKET_INVITE_PLATFORM_NAME=Ticket Admin Portal

# Redis (optional)
# REDIS_PORT=6379
